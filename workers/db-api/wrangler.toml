name = "db-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# D1 Database binding
[[d1_databases]]
binding = "JOBS_DB"
database_name = "jobs-db"
database_id = "" # Set this after running: npm run db:create

# Secrets (set with: wrangler secret put DB_AUTH_TOKEN)
# DB_AUTH_TOKEN = ""
